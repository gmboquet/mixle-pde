# Operations

PDE returns immutable capability, compatibility, solve, parity, and limitation records. Distributed scheduling,
resource policy, secrets, retries, and monitoring belong to MLOps. Preserve failed input identities and diagnostics;
never publish a partial result as supported execution.
